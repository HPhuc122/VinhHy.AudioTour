<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:VinhHy.AudioTour.Mobile.ViewModels"
             x:Class="VinhHy.AudioTour.Mobile.Views.HomePage"
             x:DataType="vm:HomeViewModel"
             Title="{Binding Title}">

    <VerticalStackLayout Padding="24" Spacing="16">
        <Label Text="Offline audio tour foundation" FontSize="18" />
        <Label Text="{Binding StatusMessage}" />
        <Label Text="{Binding PoiCount, StringFormat='POIs cached: {0}'}" />
        <Button Text="Refresh" Command="{Binding RefreshCommand}" />
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
    </VerticalStackLayout>
</ContentPage>
